from django.db import models

# TODO: Add AI analysis result models
# TODO: Add document processing status tracking
# TODO: Add AI service configuration models
# TODO: Add risk assessment results
# TODO: Add plain-language translation cache

# Example placeholder structure:
# class DocumentAnalysis(models.Model):
#     document = models.ForeignKey('documents.Document', on_delete=models.CASCADE)
#     analysis_type = models.CharField(max_length=50)
#     result = models.JSONField()
#     confidence_score = models.FloatField()
#     created_at = models.DateTimeField(auto_now_add=True)

# Placeholder – no actual models implemented yet
